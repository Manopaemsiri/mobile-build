import 'package:flutter/material.dart';

class PartnerProductSubscriptionShippingsScreen extends StatelessWidget {
  const PartnerProductSubscriptionShippingsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(
        title: Text(
          'Shippings'
        ),
      ),
    );
  }
}