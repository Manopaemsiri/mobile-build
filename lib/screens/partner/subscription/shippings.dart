import 'package:flutter/material.dart';

class PartnerProductSubscriptionShippingsScreen extends StatelessWidget {
  const PartnerProductSubscriptionShippingsScreen({super.key});

  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(
        title: Text(
          'Shippings'
        ),
      ),
    );
  }
}